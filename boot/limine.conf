# Pippin Limine configuration — reserved for the Milestone-1 boot protocol
# migration (see docs/boot.md). Not used while the kernel boots via the
# Multiboot v1 stub in kernel/asm/boot.S.

timeout: 3

/Pippin OS
    protocol: limine
    path: boot():/boot/pippin.elf